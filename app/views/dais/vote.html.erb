
<div class="container-fluid col-md-offset-1 col-md-10">

<h4>題：「<%= @dai.title %>」</h4>    
    
<table class="table table-bordered">

<%= render 'tankas/tanka_vote' %>

</table>

    <%= link_to "戻る" , @dai , class: "btn btn-sm btn-default" %>

    <% if current_user.kin_tankas.where(dai_id: params[:id].to_i).present? or
    current_user.ran_tankas.where(dai_id: params[:id].to_i).present? or
    current_user.sho_tankas.where(dai_id: params[:id].to_i).present? %>
    
    <%= link_to "投票をやりなおす" , vote_redo_path(dai_id: params[:id]) , class: "btn btn-sm btn-default" %>

</div>
    
    <% end %>
