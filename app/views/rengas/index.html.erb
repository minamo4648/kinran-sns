<div class="container-fluid col-md-offset-3 col-md-6" >
    <p>「<%= @kogyo.rengas.where(picked: true, place: @kogyo.place - 1).take.body %>」への付句</p>
    
	<p class="right_btn">
	    <% if current_user.id == @kogyo.user_id and RengaVotership.joins(:renga).where(rengas: {kogyo_id: @kogyo.id, place:@kogyo.place}).present? %>
	        <%= link_to "締切を前倒しして付句を確定する", advance_kogyo_path(id: @kogyo.id), class: "btn btn-large btn-warning" %>
	    <% end %>
	    
	    <%= link_to "戻る", root_path, class: "btn btn-large btn-default" %>
	</p>

    <p><b><%= place_name(@kogyo.place) -%></b></p>
    
    <table class="table">
        <%= render @rengas %>
    </table>

</div>