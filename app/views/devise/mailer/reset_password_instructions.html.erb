<p><%= @resource.email %>さんへ</p>

<p>短歌のサイト「KAKAI」からのお知らせです。パスワードの初期化のご案内をお送りします。</p>

<p><%= link_to 'パスワードの初期化を行う', edit_password_url(@resource, reset_password_token: @token) %></p>

<p>覚えがない場合は、このメールを無視してください。</p>
<p>上記のリンクからパスワード変更を行わない限り、現状のままになります。</p>
